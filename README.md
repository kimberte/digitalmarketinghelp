# Small Business Digital Consulting Website

Next.js marketing site designed for easy Vercel deployment.

Before launch: replace `hello@digitalmarketinghelp.ca` in `app/page.tsx` with the real business email. Add the final domain and brand name in `app/layout.tsx` and any sitemap/robots files you add later. The site intentionally has no database or environment variables for the initial launch.
