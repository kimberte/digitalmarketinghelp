import Link from "next/link"; import {articles,categories} from "@/lib/content"; import {Header,Footer,StickyHelp} from "@/app/components"; export const metadata={title:"Digital Marketing Guides",description:"Practical digital marketing guides covering Google Ads, SEO, websites, analytics, Meta Ads and more."}; export default function Guides(){return <main><Header/><section className="categoryHero"><div className="eyebrow">THE DIGITAL MARKETING LIBRARY</div><h1>Practical guides without the marketing fluff.</h1><p>Expert-written articles designed to help you understand what matters, what to fix and what to do next.</p></section><section className="section"><div className="categoryGrid">{categories.map(c=><Link className="categoryCard" key={c.slug} href={"/"+c.slug}><span>{c.name}</span><p>{c.description}</p><b>Browse →</b></Link>)}</div><div className="articleGrid" style={{marginTop:60}}>{[...articles].sort((a,b)=>b.date.localeCompare(a.date)).map(a=><Link className="articleCard" key={a.slug} href={"/articles/"+a.slug}><div className="articleMeta"><span>{a.categoryName}</span><span>{a.date}</span></div><h3>{a.title}</h3><p>{a.excerpt}</p><b>Read article →</b></Link>)}</div></section><Footer/><StickyHelp/></main>}