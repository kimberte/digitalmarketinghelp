import Link from "next/link"; import {categories,articles,getCategory} from "@/lib/content"; import {Header,Footer,StickyHelp} from "@/app/components"; import {notFound} from "next/navigation"; export async function generateStaticParams(){return categories.map(c=>({category:c.slug}))} export async function generateMetadata({params}:{params:Promise<{category:string}>}){const {category}=await params;const c=getCategory(category);return c?{title:c.name+" Guides & Articles",description:c.description}:{}} export default async function Category({params}:{params:Promise<{category:string}>}){const {category}=await params;const c=getCategory(category);if(!c)notFound();const list=articles.filter(a=>a.category===category);return <main><Header/><section className="categoryHero"><div className="eyebrow">DIGITAL MARKETING / {c.name.toUpperCase()}</div><h1>{c.name} guides and practical advice.</h1><p>{c.description}</p></section><section className="section"><div className="articleGrid">{list.map(a=><Link className="articleCard" key={a.slug} href={"/articles/"+a.slug}><div className="articleMeta"><span>{a.categoryName}</span><span>{a.date}</span></div><h3>{a.title}</h3><p>{a.excerpt}</p><b>Read article →</b></Link>)}</div></section><section className="consultBand compact"><div><p className="eyebrow">NEED PERSONALIZED HELP?</p><h2>Want someone to look at your marketing?</h2><p>Tell us what you're working on and we can help identify the right next step.</p></div><Link className="button primary" href="/contact">Get Help</Link></section><Footer/><StickyHelp/></main>}