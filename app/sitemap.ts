import type {MetadataRoute} from "next"; import {articles,categories} from "@/lib/content"; export default function sitemap():MetadataRoute.Sitemap{const b="https://digitalmarketinghelp.ca";return [{url:b},{url:b+"/guides"},{url:b+"/get-help"},{url:b+"/contact"},{url:b+"/about"},{url:b+"/tools"},...categories.map(c=>({url:b+"/"+c.slug})),...articles.map(a=>({url:b+"/articles/"+a.slug,lastModified:a.date}))]}